# Core dependencies - install first
numpy==1.24.3
scipy==1.11.4

# Core utilities
python-dotenv==1.1.0
requests==2.32.3

# ChromaDB and vector storage
chromadb==0.4.22

# OpenAI API
openai==1.82.1

# Knowledge base preprocessing
spacy==3.7.2
keybert==0.9.0
scikit-learn==1.3.2
sentence-transformers==2.2.2

# LLM inference and model support
transformers==4.35.2
# Note: torch and torchaudio versions are handled in Dockerfile for platform compatibility
huggingface-hub==0.19.4

# GGUF model support (optional - may need special installation)
llama-cpp-python==0.2.79

resemblyzer

# Speech-to-Text transcription
sounddevice==0.4.6
soundfile==0.12.1

# TTS and audio processing dependencies
# Note: torchaudio version handled in Dockerfile
librosa==0.10.1
resampy==0.4.2
# Note: deepfilternet, tts, f5-tts versions handled in Dockerfile with error handling

# Development and optional dependencies
pygame==2.6.1  # For audio playback in transcription examples

# Web framework
flask==3.0.3
flask-cors==6.0.0
psycopg2-binary==2.9.10

# Note: openai-whisper version handled in Dockerfile